[pytest]
testpaths = tests
norecursedirs =
    .git
    .pytest_cache
    __pycache__
    pytest-cache-files-*
    tmp
    temp
    database
    logs
    models
    dev
addopts = -q
markers =
    integration: marks tests as integration/manual (requires external files or real DB)
